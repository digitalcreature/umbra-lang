const std = @import("std");

pub fn main() anyerror!void {
    // var gpa = std.heap.GeneralPurposeAllocator(.{}){};
    // var args = std.process.args();
    // _ = args.skip();
    // while (args.next(&gpa.allocator)) |arg| {
    //     std.log.info("\"{s}\"", .{arg});
    // }
}
